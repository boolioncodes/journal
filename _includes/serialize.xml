{%- if include.var[0] -%} 
  {%- assign len = include.var | size -%}
  {%- if !include.var[0][0] and len == 2 -%}
      {%- assign key = include.var[0] -%}
      {%- assign val = include.var[1] -%}
    <node key="{{ key }}">
      {% include serialize.xml var=val %}
    </node>
  {%- else -%}
    <node>
    {%- for i in include.var -%}
      {% include serialize.xml var=i %}
    {%- endfor -%}
    </node>
  {%- endif %}
{%- else %}
  {{ include.var }}
{% endif -%}
